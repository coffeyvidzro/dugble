run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused

issues:
  exclude-rules:
    - path: internal/database/sqlc
      linters:
        - all
